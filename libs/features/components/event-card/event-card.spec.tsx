import '@testing-library/jest-dom';
// import {render, screen} from '@testing-library/react';
// import {EventCard} from '@mega-dev-crm/features';
// import {CalendarIcon} from '@mega-dev-crm/shared';

describe('EventCard', () => {
  it('should render successfully', () => {
    // render(
    //   <EventCard
    //     title={'Test'}
    //     count={4}
    //     icon={<CalendarIcon />}
    //     list={[]}
    //     childrenList={[]}
    //   />
    // );
    // expect(screen.getByTestId('event-card')).toBeInTheDocument();
  });

  it('should have a content', () => {
    // render(
    //   <EventCard
    //     title={'Test'}
    //     count={4}
    //     icon={<CalendarIcon />}
    //     list={[]}
    //     childrenList={[]}
    //   />
    // );
    // expect(screen.getByTestId('event-card')).toHaveTextContent('Test');
    // expect(screen.getByTestId('event-card')).toHaveTextContent('4');
  });
});
