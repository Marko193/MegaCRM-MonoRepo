import '@testing-library/jest-dom';
// import {BalanceCard} from '@mega-dev-crm/features';
// import {render, screen} from '@testing-library/react';

// const fourElementsArray = [1, 2, 3, 4];

describe('BalanceCard', () => {
  it('check whether there is a collapse block with 4 children', () => {
    // render(
    //   <BalanceCard title='balanceCard.available.balance'>
    //     {fourElementsArray.map((item, index) => {
    //       return <div key={index}>{item}</div>;
    //     })}
    //   </BalanceCard>
    // );
    // expect(
    //   screen.getByTestId('balance-card-expanded-items')
    // ).toBeInTheDocument();
  });

  it('should have 1 element at collapse block', () => {
    // render(
    //   <BalanceCard title='balanceCard.available.balance'>
    //     {fourElementsArray.map((item, index) => {
    //       return <div key={index}>{item}</div>;
    //     })}
    //   </BalanceCard>
    // );
    // expect(
    //   screen.getByTestId('balance-card-expanded-items').children.length
    // ).toBe(1);
  });
});
