import '@testing-library/jest-dom';
// import { render, screen } from '@testing-library/react';
// import { CardRange } from './cardRange';

describe('Card range', () => {
  it('Should be rendered without slider', () => {
    // render(
    //   <CardRange
    //     startDate=""
    //     endDate=""
    //     startLabel="cardRange.fromLabel"
    //     endLabel="cardRange.tillLabel"
    //   />
    // );
    // expect(screen.getByTestId('card-range')).toBeInTheDocument();
  });
  it('Should be rendered with slider', () => {
    // render(
    //   <CardRange
    //     startDate=""
    //     endDate=""
    //     startLabel="cardRange.fromLabel"
    //     endLabel="cardRange.tillLabel"
    //     rangeSlider={true}
    //   />
    // );
    // expect(screen.getByTestId('card-range')).toBeInTheDocument();
    // expect(screen.getByTestId('slider')).toBeInTheDocument();
  });
});
