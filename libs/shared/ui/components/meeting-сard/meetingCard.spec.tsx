import '@testing-library/jest-dom';
// import { render, screen } from '@testing-library/react';
// import { CircleQuantity, MeetingCard } from '@mega-dev-crm/shared';

describe('MeetingCard', () => {
  it('should render successfully', () => {
    // render(
    //   <MeetingCard
    //     title={'probation.meeting'}
    //     children={<CircleQuantity quantity={3} circleSize={40} />}
    //   />
    // );
    // expect(screen.getByTestId('circle-quantity')).toBeInTheDocument();
    // expect(screen.getByTestId('meetingCard-title')).toBeInTheDocument();
  });

  it('should have content', () => {
    // render(
    //   <MeetingCard
    //     title={'probation.meeting'}
    //     children={<CircleQuantity quantity={3} circleSize={40} />}
    //   />
    // );
    // expect(screen.getByTestId('circle-quantity')).toHaveTextContent('3');
    // expect(screen.getByTestId('meetingCard-title')).toHaveTextContent(
    //   'probation.meeting'
    // );
  });
});
