import '@testing-library/jest-dom';
// import { render, screen } from '@testing-library/react';
// import { CustomChips } from '@mega-dev-crm/shared';

describe('CustomChips', () => {
  it('should render successfully', () => {
    // render(<CustomChips title={'vue.js'} />);
    // expect(screen.getByTestId('custom-chip')).toBeInTheDocument();
  });

  it('should have content', () => {
    // render(<CustomChips title={'vue.js'} />);
    // expect(screen.getByTestId('custom-chip')).toHaveTextContent('vue.js');
  });
});
